/*
 * void X_sort(ElementType a[], int n);
 * 大多数情况下，为了方便起见，讨论从小到大的整数排序
 *
 * n是 正 整数
 * 只讨论基于比较的排序（> = <有定义）
 * 只讨论 内部 排序 (内存空间足够大，数据一次导入内存, 对应的还有 外部排序)
 * 稳定性：任意两个相等的数据，排序前后的相对位置不发生变化
 * 没有一种排序是在任何情况下都表现最好的
 * 
 * */
